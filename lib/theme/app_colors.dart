import 'package:flutter/material.dart';

class AppColors {
  // =====================
  // PRIMARY COLORS
  // =====================
  static const Color primary = Color(0xFF1E88E5);
  static const Color primaryLight = Color(0xFFE3F2FD);
  static const Color primaryDark = Color(0xFF1565C0);

  // =====================
  // BACKGROUND
  // =====================
  static const Color background = Color(0xFFF8FBFF);
  static const Color card = Colors.white;
  static const Color onboardingBackground = Color(0xFFEAF4FF);

  // =====================
  // TEXT COLORS
  // =====================
  static const Color textPrimary = Color(0xFF263238);
  static const Color textSecondary = Color(0xFF607D8B);
  static const Color textHint = Color(0xFF90A4AE);

  // =====================
  // BUTTON
  // =====================
  static const Color buttonPrimary = primary;
  static const Color buttonText = Colors.white;
  static const Color buttonDisabled = Color(0xFFB0BEC5);

  // =====================
  // PRICE & STATUS
  // =====================
  static const Color price = Color(0xFFD32F2F);
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color error = Color(0xFFC62828);

  // =====================
  // ICON & BORDER
  // =====================
  static const Color iconActive = primary;
  static const Color iconInactive = Color(0xFF90A4AE);
  static const Color border = Color(0xFFE0E0E0);

  // =====================
  // NAVBAR
  // =====================
  static const Color navBarBackground = Colors.white;
  static const Color navBarActive = primary;
  static const Color navBarInactive = Color(0xFFB0BEC5);
}
